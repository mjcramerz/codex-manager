# Secrets And Auth Overview
Purpose: route you to the installed docs for Codex login tokens, MCP tokens, and lookup-file handling.

## Start here
- `secrets.md` — `secrets.toml`, `auth.toml`, `codex-login`, and `codex-mcp-token`
