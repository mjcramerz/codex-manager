{{ title }}

This rollup is generated by Codex memory phase 5 for production use.

Use the summary block for a compact snapshot of workspace coverage across the current selection.
Use the sections block for newest-first workspace rollups based on the latest selected phase-1 entry for each workspace.

## Summary
{{ summary_block }}

## Workspace Rollups
{{ sections_block }}

## Customization Notes
- Keep `{{ title }}`, `{{ summary_block }}`, and `{{ sections_block }}` intact when customizing this layout.
- These rollups are deterministic summaries of the selected phase-1 outputs, not freeform agent-authored prose.
