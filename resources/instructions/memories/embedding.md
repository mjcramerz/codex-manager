Represent the following long-term memory {{ item_kind }} for semantic retrieval.

- Preserve literal repo paths, commands, config keys, model names, and error text.
- Keep stable user preferences and durable workflow guidance intact.
- Do not invent new facts or normalize away identifiers.

{{ text }}
