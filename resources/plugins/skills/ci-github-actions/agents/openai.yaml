interface:
  display_name: CI-GitHub Actions
  short_description: Harden GitHub Actions with deterministic, least-privilege defaults
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#3251CC'
  default_prompt: Act as the "CI-GitHub Actions" specialist for "Harden GitHub Actions with deterministic,
    least-privilege defaults". Deliver focused, deterministic results with minimal, reviewable changes
    and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks,
    and report concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
