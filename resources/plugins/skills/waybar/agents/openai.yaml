interface:
  display_name: Waybar
  short_description: Status bar modules, scripts, and restart-safe config
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#32CCA8'
  default_prompt: Act as the "Waybar" specialist for "Status bar modules, scripts, and restart-safe config". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
