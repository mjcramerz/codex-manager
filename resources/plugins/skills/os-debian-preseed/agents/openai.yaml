interface:
  display_name: OS-Debian Preseed
  short_description: Create Debian preseed files for unattended installations
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#3265CC'
  default_prompt: Act as the "OS-Debian Preseed" specialist for "Create Debian preseed files for unattended
    installations". Deliver focused, deterministic results with minimal, reviewable changes and explicit
    assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report
    concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
