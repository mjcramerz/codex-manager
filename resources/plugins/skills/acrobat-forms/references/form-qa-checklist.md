# Form QA checklist

Before final handoff:
- confirm required fields are populated
- verify names, dates, and identifiers match provided source data
- check signatures/initials are present where required
- confirm the exported file opens cleanly and preserves visible entries
- note any manual submission or review steps that still remain
