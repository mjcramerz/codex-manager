interface:
  display_name: STORAGE-Filesystems
  short_description: 'Safe filesystem planning: partitioning, mkfs, mounting, and fstab with explicit confirmation and rollback'
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#7032CC'
  default_prompt: 'Act as the "STORAGE-Filesystems" specialist for "Safe filesystem planning: partitioning, mkfs, mounting, and fstab with explicit confirmation and rollback". Deliver focused, deterministic results with minimal, reviewable
    changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant
    checks, and report concrete actions, evidence, and residual risks.'

policy:
  allow_implicit_invocation: true
