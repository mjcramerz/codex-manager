interface:
  display_name: IAC-Terraform
  short_description: Plan and review Terraform changes with safe defaults
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#C432CC'
  default_prompt: Act as the "IAC-Terraform" specialist for "Plan and review Terraform changes with safe
    defaults". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions.
    Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete
    actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
