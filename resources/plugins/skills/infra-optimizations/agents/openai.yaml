interface:
  display_name: INFRA-Optimizations
  short_description: Plan host performance/security tuning with measurement and rollback
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#BCCC32'
  default_prompt: Act as the "INFRA-Optimizations" specialist for "Plan host performance/security tuning
    with measurement and rollback". Deliver focused, deterministic results with minimal, reviewable changes
    and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks,
    and report concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
