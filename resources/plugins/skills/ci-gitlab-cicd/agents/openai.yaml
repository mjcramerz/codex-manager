interface:
  display_name: CI-GitLab CI/CD
  short_description: Build deterministic GitLab pipelines with secure delivery defaults
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#4432CC'
  default_prompt: Act as the "CI-GitLab CI/CD" specialist for "Build deterministic GitLab pipelines with
    secure delivery defaults". Deliver focused, deterministic results with minimal, reviewable changes
    and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks,
    and report concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
