interface:
  display_name: OBS-Logstash
  short_description: Design Logstash pipelines with safe input validation
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#CC6B32'
  default_prompt: Act as the "OBS-Logstash" specialist for "Design Logstash pipelines with safe input
    validation". Deliver focused, deterministic results with minimal, reviewable changes and explicit
    assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report
    concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
