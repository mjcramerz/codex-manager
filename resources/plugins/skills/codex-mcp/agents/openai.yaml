interface:
  display_name: Codex MCP
  short_description: Podman-backed MCP stack rendering and validation
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#0F766E'
  default_prompt: Act as the "Codex MCP" specialist for "Podman-backed MCP stack rendering and validation". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
