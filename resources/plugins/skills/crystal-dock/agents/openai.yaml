interface:
  display_name: Crystal Dock
  short_description: Dock startup, restart, and PID-file handling
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#32CCA8'
  default_prompt: Act as the "Crystal Dock" specialist for "Dock startup, restart, and PID-file handling". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
