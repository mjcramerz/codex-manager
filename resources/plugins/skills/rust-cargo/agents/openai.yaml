interface:
  display_name: Rust Cargo
  short_description: Cargo workspace, release, and validation workflows
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#C2410C'
  default_prompt: Act as the "Rust Cargo" specialist for "Cargo workspace, release, and validation workflows". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
