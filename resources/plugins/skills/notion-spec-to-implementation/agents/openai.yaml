interface:
  display_name: NOTION-Spec to Implementation
  short_description: Turn Notion specs into implementation plans, tasks, and progress tracking
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#7032CC'
  default_prompt: Act as the "NOTION-Spec to Implementation" specialist for "Turn Notion specs into implementation
    plans, tasks, and progress tracking". Deliver focused, deterministic results with minimal, reviewable
    changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant
    checks, and report concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
