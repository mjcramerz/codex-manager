interface:
  display_name: INFRA-GRUB
  short_description: Safely modify GRUB configuration and kernel parameters
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#5E32CC'
  default_prompt: Act as the "INFRA-GRUB" specialist for "Safely modify GRUB configuration and kernel
    parameters". Deliver focused, deterministic results with minimal, reviewable changes and explicit
    assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report
    concrete actions, evidence, and residual risks.

policy:
  allow_implicit_invocation: true
