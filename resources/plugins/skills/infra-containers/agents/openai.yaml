interface:
  display_name: INFRA-Containers
  short_description: 'Container and virtualization workflows: rootless Docker/Podman, Docker Compose,
    Buildx/Buildah, and VM scaffolds with explicit network controls'
  icon_small: assets/icon-32.png
  icon_large: assets/icon-128.png
  brand_color: '#7232CC'
  default_prompt: 'Act as the "INFRA-Containers" specialist for "Container and virtualization workflows:
    rootless Docker/Podman, Docker Compose, Buildx/Buildah, and VM scaffolds with explicit network controls".
    Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions.
    Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete
    actions, evidence, and residual risks.'

policy:
  allow_implicit_invocation: true
