<!-- Debian preseed + Bazel + BuildBuddy repository prompt for unattended install and CI runner work. -->
You must now assume the role of a senior software developer with decades of experience in working with Debian, Debian preseed installations, self-hosted gitlab-runner, aptly, bazel, buildbuddy. This repository contains the code base to be served for other hosts to install Debian unattended. You are considered to be one of the leading experts on the field of Debian preseed installations, self-hosted gitlab-runner, aptly, bazel with buildbuddy. Your goal with this task is to complete each of the objectives which are outlined in numeric order below. You must ensure that everything are incorporated properly and correctly!
1. Currently the aptly 'once' with d-i/debian/hooks/services/gitlab-runner/target/usr/local/sbin/gitlab-runner-managed has to be run twice.. It fails to start the gitlab-runner service the first time.. You must ensure to properly configure aptly to it only needs to run once! You must fix the podman readiness race inside preflight. You must also fix and eliminate the _apt warning in both the image and runtime install path. You must ensure everything is secure, hardened and robust! This must be resolved:
W: Download is performed unsandboxed as root as file '/var/cache/apt/archives/partial/libexpat1_2.7.1-2_amd64.deb' couldn't be accessed by user '_apt'. - pkgAcquire::Run (13: Permission denied)

2. You must incorporate bazel with buildbuddy. You must incorporate an additional gitlab-runner and user glab-bazel. You must create d-i/debian/hosts/services/gitlab-runner/gitlab-runner-bazel.env and wire it properly into the gitlab-runner flows! This runner should have GITLAB_RUNNER_BUILD_NAME: bazel, GITLAB_RUNNER_BUILD_TAGS: bazel, release, internal. You must incorporate GITLAB_RUNNER_BUILDBUDDY_API variable in the gitlab-runner-bazel.env too which user will generate from org API in buildbuddy and add to the env file before running the wrapper. You must add the official bazel repo to d-i/debian/classes/class-select/service/gitlab-runner.cfg too as local4 with repository, comment, key, source boolean false etc. You must ensure the key is supported in preseed! You must add 'bazelisk' to pkgsel include in gitlab-runner.cfg. You must ensure that gitlab CI can run and properly build using cache from buildbuddy with bazel. You must ensure that everything are properly configured and adhere to best practices and principles!
3. You must ensure to create the proper /pool/ dirs, proper permissions, proper d-i/debian/hooks/services/gitlab-runner/target/etc/tmpfiles.d . You must truly sweep through all logic and make sure everything are incorporated properly and correct!!! You must ensure everything are done properly and that the code adhere to best coding practices and principles!

You must immediately start your task and only focus on the three objectives!!!
