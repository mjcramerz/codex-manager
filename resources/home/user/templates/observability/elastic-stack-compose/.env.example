ELASTIC_VERSION=8.0.0
# If you bind-mount host paths, set ENGINE_UID/ENGINE_GID to the engine user.
ENGINE_UID=
ENGINE_GID=
APP_UID=
APP_GID=
DEV_UID=
DEV_GID=
# Tip: use `./rootless_env.sh --dotenv > .env`.
# Podman rootless: use compose.podman.override.yml (keep-id) for bind mounts.
